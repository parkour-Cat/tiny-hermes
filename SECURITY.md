# 安全漏洞报告

## 怎么报告

**不要开公开 issue。** 请发邮件到项目维护者，或使用 GitHub 的 private vulnerability
reporting。请包含：

- 受影响的组件与版本（commit）
- 复现步骤，或一个能说明问题的最小用例
- 你认为的影响范围

我们会在收到后确认，并在修复发布前与你商定披露时间。

## 这个平台把什么当作安全边界

报告前有必要知道我们**认为**哪些线是线，因为踩过线才算漏洞：

- **工作空间隔离。** 用 A 的身份拿到 B 的任何资源字段，都是漏洞。
- **终端用户之间的隔离。** 私有会话、文件和记忆按主体隔离，`MemoryScope` 在类型上
  就表达不出「所有主体」。
- **出站边界。** 平台内一切出站流量经过 `egress-proxy`；能从沙箱或工具直连到
  loopback、link-local、云元数据地址或未批准网段，是漏洞。
- **Secret 的信封加密。** 只拿到数据库转储而没有部署侧 KEK 就应当解不开
  （`scripts/backup_restore_drill.py` 每次都验证这一点）。明文出现在提示词、日志或
  RunEvent 里，是漏洞。
- **审批绑定。** 一次审批只覆盖被批准的那一个调用；改了工具名、参数、工作目录、
  目标或权限之后仍被放行，是漏洞。
- **已发布版本不可变。** 能原地改一个已发布的 AgentVersion 或 SkillVersion，是漏洞。

## 已知不是漏洞的

- **平台信任企业签发方的签名，不验证事实。** 终端用户的身份由企业 JWT 担保；企业签了
  一个不属于它的 `sub`，平台会接受。这是 §4.5.1 的设计，不是缺陷。
- **停用签发方不会踢掉已换出的会话**（最长一个会话周期）。这是文档化的权衡，
  需要立刻断开时有单独的撤销接口。
- **回滚 schema 会不可逆删除部分数据**，见 `docs/operations.md`。
